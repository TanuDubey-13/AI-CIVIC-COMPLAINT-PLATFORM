exports.getComplaints = (req, res) => {
  res.json({ message: 'Complaint list controller ready' });
};

exports.createComplaint = (req, res) => {
  res.json({ message: 'Complaint create controller ready' });
};
